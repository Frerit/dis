import 'package:flutter/material.dart';

class SignPlanPage extends StatelessWidget {
  const SignPlanPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
