class DistTask {
  final String id;
  final String title;
  final String subtitle;

  final double total;
  final double completed;

  DistTask(this.id, this.title, this.subtitle, this.total, this.completed);
}
